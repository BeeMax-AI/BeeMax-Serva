# 实现范围与验收

依据：`design/gongdan-dashboard/设计说明.md` 和用户本次指令「先实现前端、后端，后续提供 MCP Server 对接数据库」。保留现有工单运营功能，不增加会话中心、知识库或其他底座产品模块。

当前为可运行的本地全栈应用，不是已接入客户数据库的生产系统。原设计稿保留；浏览器从 Node API 获取真实持久化的本地记录。UI 维持千蜂智服品牌、顶部 AI智能分析、连接管理分组和全局浮动 AI助手。

## 范围

实现入口和说明见 README。核心边界：

- `shared/domain.ts`：规范化客户数据、配置、工单、消息、分析和会话类型。
- `server/src/provider.ts`：后端本地提供器、校验、幂等、版本冲突及 MCP 待接入边界。
- `server/src/http.ts`：会话、角色、同源检查与 API。
- `server/src/store.ts`：仅用于本地开发的 SQLite、账号及加密凭据保存。
- `server/src/ai.ts`、`dates.ts`、`metrics.ts`：真实记录聚合、可选模型、周期调度与报告保存。
- `client/src/`：数据驱动的前端页面、操作确认和浮动助手。

前端分组和名称由客户数据提供；用户/租户和审计操作人只从后端会话取值。未配置 MCP 不访问原规格中的网络地址。界面没有切换 owner 角色的设计预览按钮。

## 验证记录

2026-09-06：TypeScript 检查、32 项 HTTP/周期/模型边界测试、客户端构建及 BeeMax 品牌色检查通过。测试使用独立临时数据目录。

浏览器实测：

- admin 登录；路由关键词修改经过确认后保存，刷新仍保留。
- 工单 E174 催办追加后端时间线；工单详情可带入 AI助手并查询 E169。
- 生成自定义日期报告，刷新后历史报告保留；自定义十天分析计划保存后显示下次运行时间。
- 白名单添加、确认、移除；消息搜索 E169 命中两条入站/出站记录。
- 管理员的连接凭据页仅显示只读状态；owner 写权限及明文不泄露由 HTTP 测试验证。
- 助手放大后实际鼠标拖拽由 960×820 调整为 827×716；切换还原与放大后保留尺寸和草稿。
- 390px 窄屏检查总览、账号、Agent、消息、配置页，无页面级横向溢出；表格保留局部滚动；图表改用窄屏坐标尺寸。
- 浏览器错误日志为空。服务以最新代码重启，登录会话与已保存数据仍可用。

审查结果分别处理：

- Standards：修复转派改写首次接单时间、跨期建议状态副本复活两项问题。
- Spec：修复跨期建议、助手只发送页面名称而没有业务上下文、查询中继续输入被回复清空三项问题；复查修复报告切换时助手标题不同步。
- 额外核对：确认表单保留打开时的版本，避免后台刷新让旧表单绕过冲突检查；自定义计划确认展示间隔、首次日期、时区与范围。

截图见 `docs/screenshots/overview.png`、`insights.png`、`mobile-overview.png`。该截图来自初始本地版本。2026-09-06 已接入 MCP 并完成真实只读验收；QiWe 独立通道和外部模型仍未配置，具体边界见 mcp-integration.md。

### 独立 QiWe 连接配置

设置中分别保留「QiWe 连接」与「MCP 数据连接」。`GET /api/qiwe/connection` 返回已保存字段、脱敏账号、更新时间、独立版本号；`POST` 使用 `expectedRevision` 更新凭据，留空保留旧值，运营管理员（admin）及平台管理员（owner）均可修改，只读账号不可修改。接口在读取业务数据源之前处理，因此不依赖 MCP 工具、业务数据版本或 MCP 可用性。凭据沿用服务端 AES-256-GCM 加密存储，操作记录只记动作，不记录内容。

当前未提供 QiWe API 地址、鉴权及连接检测协议，因此仅实现凭据管理；页面始终明确显示「未验证」，检测按钮不可用。后续拿到 QiWe 文档后接独立服务端适配器，不通过工单 MCP 修改 QiWe 配置，也不把本地保存成功当作远端连接成功。

### 企微连接管理设计稿迁移

2026-09-06：依据 `design/gongdan-dashboard/connections.js` / 8765 的企微账号页，将连接管理作为独立配置模块迁入正式应用。

| 设计稿功能 | 正式平台实现 |
| --- | --- |
| 添加企微账号、管理实例 | 名称、所属空间持久化；只由服务端设置待连接/未登录状态 |
| 账号筛选、Agent 详情 | 账号列表与详情互通，保留 20/30 条分页 |
| 主动推送群名单 | 按实例添加、移除 chat_id 与群名；配置保存，通道接入后才能生效 |
| 消息日志 | 实例/方向/类型/状态/关键词筛选、详情、分页、15 秒独立刷新；当前无真实通道消息 |
| 连接设置 | 进入独立 QiWe 凭据页面；返回企微账号，保持真实角色权限 |
| 操作记录 | 连接配置审计合并展示到工作台变更记录 |

`GET /api/connections`、`POST /api/connections/commands` 均在业务数据源调用前处理，SQLite `wecom_connections` 按登录租户隔离，独立版本号与请求幂等控制。账号/群配置允许运营管理员及平台管理员修改，只读账号不能写入。初始为空，不将设计稿的酒店名称、示例账号、模拟在线状态或消息当成正式数据。旧本地开发数据提供器的账号/消息字段不作为此模块的数据来源。

连接刷新使用专属事件，不中止或重置 MCP 同步；较旧的连接或工作台响应不能覆盖已保存的新连接配置。企微登录、真实在线检测、主动推送执行和消息接收仍待 QiWe 服务契约接入，当前界面明确显示待连接/待同步。

迁移验证：81 项测试、类型检查和客户端构建通过。隔离浏览器验证新增账号、推送群保存、刷新后保留、配置页联动与消息日志筛选入口；回归覆盖两种数据模式的独立保存、租户/只读权限、重复提交、配置版本冲突和过期响应保护。

2026-09-06：按用户要求开放 QiWe 凭据编辑给运营管理员，与平台管理员共用同一保存权限；只读账号仍由服务端拒绝。表单按参考图改为 Token / 账号 / 密码三行布局，Token 与密码可分别显示/隐藏，重置恢复隐藏状态。此变更不提升账号角色或其他配置权限。

2026-09-07：QiWe 连接页统一使用配置管理的全宽面板、标题和左侧对齐网格，表单内部限制输入宽度并缩减间距。账号管理入口移至标题区，连接状态与检测说明合并，底部仅保留输入提示、重置和保存。窄屏使用单列表单与换行操作区；已验证桌面、390px 窄屏、显隐及重置交互，104 项测试通过。保存权限与未验证状态保持原有语义，未提交真实凭据。

### 2026-09-07 白名单统一归入企微账号

企微实例的 Agent 详情入口改为「白名单」，包含白名单授权（群 / 人员）、推送设置、访问权限。移除配置管理的「群与权限」「推送群设置」入口。实例群和人员授权独立持久化；推送开关仅面向已授权群，新群默认关闭推送，旧记录缺少开关时保留原有开启语义。撤销群授权同步移除该群的推送配置。

访问权限继续复用 MCP 渠道接口，明确标注整个渠道生效，不将实例授权默认为已同步；企微账号列表保留渠道权限入口，空实例时仍可操作原有 MCP 权限。新增人员及推送操作沿用后端角色、租户、版本与请求幂等检查。

验证：83 项测试、类型检查、构建通过；规范 / 规格双轴审查无可操作问题。隔离浏览器验证账号入口、人员添加、推送开关保存，以及桌面和窄屏布局。未变更真实群、人员权限或发送外部消息。

白名单模块进一步内嵌到企微账号列表下方，三个页签始终可见；实例选择器指定授权与推送的管理对象，账号行的白名单按钮定位到页面下方。旧 `#agent` 链接兼容显示同一账号页面。账号列表与白名单使用独立分页状态，MCP 渠道级范围保持不变。

### 2026-09-07 升级通知人员管理

排班人员页新增按小组的 L1–L4 通知名单，展示中文职责、真实升级间隔、默认/今日来源和 @所有人规则。管理员可选择默认持续名单或当前业务日期覆盖，对指定级别添加、移除人员；添加支持选择已知人员或填写真实企微 ID，移除前明确级别、对象、范围及空名单提示。人员与负载页提供直接入口。

后端以最新完整 MCP 排班快照修改目标级别，保护日期覆盖、其他级别、小组、楼栋、时段和专员配置；异常排班容器与广播级别不允许个人修改。保留版本检查、操作审计、跨日提交拒绝及请求防重复机制。89 项测试、类型检查、构建和品牌色检查通过。实际浏览器验证三个小组选择入口、L1–L4 展示、添加表单及移除确认后取消；未修改真实名单。

L3 增加「编辑通知方式」，可在部门全体（@所有人）与指定人员之间切换，指定名单支持勾选已知人员及直接填写新人员。确认页对比原设置和新名单，明确默认/今日范围；L3 的广播模式仍不能用普通个人增删命令隐式修改。独立 `roster.level.save` 命令校验级别、范围、日期、人数、重复 ID 和广播 ID，通过 `set_roster` 保存完整快照；保留未变动成员元数据。93 项测试通过，真实名单未修改。

### 功能模块归并

「人员与负载」通过页签管理人员负载与排班人员；排班、逐级通知、今日当班和导入排班已从配置管理移入此处。新增 `#staff-roster` 直接入口，保留原权限校验与分页。

「工单管理」合并原工单明细与工单趋势导航，通过页签切换。`#tickets`、`#trends` 保留，刷新与历史导航仍指向对应内容，侧栏统一选中工单管理。AI 助手上下文同步显示所属模块和当前功能。

「派单参数」现作为人员与负载的第三个页签，入口为 `#staff-parameters`，配置管理不再重复展示。升级间隔、完成提醒和挂起提前提醒的原有编辑、权限及确认流程保留。已验证直接打开和编辑弹窗；未修改实际参数，93 项测试通过。

### 运营总览新增数据维度

总览下方新增未闭环时长分布、小组处理效率、新单高峰时段、高频问题 Top 5。积压取当前未闭环状态，按创建至当前分段（含挂起），无效或未来创建时间归为待核对；其余统计支持近 7/30 天，使用 UTC+8 日界线。

小组效率展示当前待接单、待闭环，以及期间接单/闭环样本的响应/处理时长中位数和有效样本量。排除缺失、逆序及未来时间；处理时长包含挂起。时段和类型按期间新建记录统计，占比以全部期间新单为分母。数据范围明确为已同步记录。

数量与有效时长可跳转对应工单集合，维度、日期和计算时刻保存在链接中，刷新仍保留筛选，可清除或重置。没有修改业务数据或调用写入工具。已验证桌面/窄屏显示、7/30 天切换、类型和时段数量跳转、直接刷新及重置。新增 6 项计算/边界测试，总计 99 项测试通过。

### 工单趋势增加结构与效率分析

工单趋势新增每日响应时长中位数、处理时长中位数、每日新建与闭环差额，以及可选择最多三个小组的每日新建趋势。原有类型和小组分布补充占比、上期数量与变化；日、周、月分别使用 1、7、30 个自然日，上期按相同天数及当日相同进度比较，统计范围可展开查看。

响应样本按接单日期归集，处理样本按闭环日期归集并包含挂起时间；排除缺失、逆序及未来时间。没有样本显示断点，有效零时长保留。数据仅代表已同步记录，上期未记录时不计算涨幅，新建减闭环的差额不作为完成率或历史积压存量。

折线数据点、分布数量与每日明细数量可跳转到对应工单集合，刷新保留筛选。每日明细支持 10/20/30 条分页，翻页及修改页大小保留展开状态。实际浏览器验证日/月切换、小组选择、月度明细翻页与每页 10 条、响应样本跳转；104 项测试、类型检查和构建通过，审查发现的明细翻页收起问题已修复并复核。没有修改业务数据。

布局优化：保留顶部工单流转趋势，下方按「服务效率」「工单结构」「小组与增减」切换，每次展示两个相关面板；默认服务效率。卡片按内容高度排列，统计说明移至末尾折叠区。六个原有面板及数据口径完整保留，切换维度时保持明细展开和分页位置。已验证桌面/390px 窄屏、维度切换、月度明细分页和焦点恢复；107 项测试通过。

### AI 报告建议跟进

当前报告下方增加待评估、跟进中、已完成和待复盘数量，以及包含优先级、负责人、复盘日期、状态和证据展开的跟进清单。待复盘仅统计跟进中且有效复盘日期不晚于当前业务日期的建议，是跟进中的子集；不同报告不合并计数。同 ID 建议保留首条，与现有操作查找行为一致。

清单优先展示到期事项、未完成事项与高优先级建议；复用现有安排/完成跟进和权限判断。每份报告拥有独立分页位置，默认每页 10 条。已验证桌面/390px 窄屏、历史报告切换、依据展开和安排跟进弹窗后取消，未提交真实跟进变更。107 项测试、类型检查和构建通过。

AI 分析页进一步合并重复建议区域，统一为「建议行动与跟进」。顶部用原文摘录、已有主要发现和未完成建议中的优先行动提供三项重点，完整结论和所有发现仍可展开；前端不生成新的分析结论。点击待评估、跟进中、已完成或待复盘数量可筛选清单，查看全部恢复；筛选按报告独立保存，切换筛选回到第一页，总数保持全报告统计。已完成事项仍支持重新跟进，权限和确认流程保留。桌面和390px窄屏、空态与全部恢复、安排弹窗已验证，109 项测试通过，未提交真实建议变更。

### PDF 运营简报

「导出简报」改为下载 A4 竖版两页 PDF，沿用 BeeMax 蓝色与品牌图标。第一页包含期间新建、期间闭环、当前待闭环及待接单 / 挂起指标，矢量流转趋势、类型与小组分布；第二页精选前 5 条重点工单、前 5 位有接单或待办的人员。较长文本限制行数并省略，空数据保留明确空态。

`GET /api/brief.pdf` 仅供已登录用户，按 actor 租户读取业务数据。界面按当前日 / 周 / 月导出 1 / 7 / 30 天；接口校验有效日期、先后顺序、不能晚于当前业务日以及最多 31 天。日期按 UTC+8，期间流量与当前状态快照分别说明；无效、未来或逆序时间不计入对应有效流量。当前待办包含所有非 CLOSED 状态，不把新建减闭环当作完成率。重点事项与人员摘要并非完整清单或绩效排名。

PDF 在内存生成，不保存到业务数据库，响应禁止缓存。中文采用随仓库分发的 OFL 字体并内嵌子集，不依赖系统字体。前端提供生成状态、防重复点击和超时提示；退出或切换身份后丢弃旧请求结果。

验证：114 项测试、类型检查及构建通过，包括 UTC+8 边界、日期校验、空 / 长文本双页渲染、登录权限、租户隔离与跨会话迟到响应。真实页面按钮下载成功，下载文件约 105 KB，两页 A4，逐页渲染检查无文字截断或表格重叠；仅执行读取与下载，未修改远端业务数据。规格与规范双轴审查发现的异步身份问题已修复并复核。
